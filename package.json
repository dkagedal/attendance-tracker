{
  "name": "attendance-tracker",
  "version": "2.0.0",
  "description": "## An attendance tracker web app",
  "main": ".",
  "author": "David Kågedal",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "build": "npm run build -w frontend && npm run build -w backend",
    "serve": "npm run serve -w backend",
    "dev": "npm run build && firebase emulators:start --project demo-project --import packages/backend/testdata",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^14.18.0"
  }
}