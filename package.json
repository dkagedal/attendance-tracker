{
  "name": "attendance-tracker",
  "version": "2.0.0",
  "description": "## An attendance tracker web app",
  "main": ".",
  "author": "David Kågedal",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "build": "npm run build -w frontend && npm run build -w backend",
    "serve": "npm run serve -w backend",
    "dev": "ln -sf config-emulator.ts packages/frontend/src/config.ts && npm run build && firebase emulators:start --project demo-project --import packages/backend/testdata --export-on-exit",
    "deploy-all": "ln -sf config-live.ts packages/frontend/src/config.ts && firebase deploy",
    "deploy-live": "ln -sf config-live.ts packages/frontend/src/config.ts && npm run build && firebase deploy --only hosting",
    "deploy-staging": "ln -sf config-staging.ts packages/frontend/src/config.ts && npm run build && firebase deploy",
    "deploy-test": "ln -sf config-test.ts packages/frontend/src/config.ts && npm run build && firebase hosting:channel:deploy test",
    "seed": "npm run seed -w backend"
  },
  "devDependencies": {
    "firebase-tools": "^14.18.0"
  }
}